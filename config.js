window.YPR_CONFIG = {
  bookingEndpoint: "",
  businessEmail: "bookings@yourrpartyrentals.com",
  businessPhone: "+15551234567",
  paymentCheckoutUrl: "",
  // Set to your deployed backend URL (e.g. "https://your-app.onrender.com") once server.py is hosted somewhere that runs it continuously.
  // Leave blank to call the API on the same origin the page was loaded from (only works if that origin also runs server.py).
  apiBaseUrl: "",
};
